{
    "tasks": [
        {
            "type": "shell",
            "label": "Crear carpeta build",
            "command": "mkdir",
            "args": ["-p", "${fileDirname}/build"],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": []
        },
        {
            "type": "cppbuild",
            "label": "Compilar C++20, g++ V.13.3.0",
            "command": "/usr/bin/g++",
            "args": [
                "-fdiagnostics-color=always",
                "-g",
                "-std=c++20",
                "${file}",
                "-o",
                "${fileDirname}/build/${fileBasenameNoExtension}"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "dependsOn": "Crear carpeta build",
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger in Linux with C++20."
        }
    ],
    "version": "2.0.0"
}